# 养基宝浏览器插件使用说明

## 插件简介

这是一个用于查看基金实时收益的浏览器插件，支持基金持仓展示、账户汇总、定时刷新、企业微信机器人推送、日报、周报、月报和接口测试。

当前版本：1.2.1

## 目录说明

- `manifest.json`：浏览器插件配置文件
- `popup.html`：插件弹窗页面
- `css/`：样式文件
- `js/`：插件脚本
- `assets/`：图片和静态资源
- `REMARD.md`：插件使用说明

## Google Chrome 安装方式

1. 打开 Chrome 浏览器。
2. 地址栏输入 `chrome://extensions/` 并回车。
3. 打开右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择本项目目录：`yjb-plugin-1.1.4`。
6. 安装完成后，在浏览器扩展图标中固定插件。
7. 点击插件图标，按页面提示登录并查看数据。

## Microsoft Edge 安装方式

1. 打开 Edge 浏览器。
2. 地址栏输入 `edge://extensions/` 并回车。
3. 打开左侧或右侧的“开发人员模式”。
4. 点击“加载解压缩的扩展”。
5. 选择本项目目录：`yjb-plugin-1.1.4`。
6. 安装完成后，在浏览器扩展图标中固定插件。
7. 点击插件图标，按页面提示登录并查看数据。

## 企业微信推送配置

1. 打开插件弹窗。
2. 进入设置。
3. 打开“企业微信推送”配置。
4. 填写企业微信机器人 Webhook 地址。
5. 按需开启定时推送、日报、周报、月报。
6. 点击对应测试按钮确认推送效果。
7. 点击保存设置。

## 数据刷新

1. 打开插件设置。
2. 在“数据刷新”中开启或关闭定时刷新。
3. 选择刷新间隔。
4. 插件会按设置刷新账户、基金持仓和行情数据。

## 周报和月报数据测试

设置面板中提供收益接口测试：

- `测试 day`
- `测试 week`
- `测试 month`

测试结果会显示接口返回的原始 JSON，可用于核对接口字段和周期收益数据。

## GitHub 上传建议

建议上传以下内容：

- `assets/`
- `css/`
- `js/`
- `manifest.json`
- `popup.html`
- `package.json`
- `package-lock.json`
- `.gitignore`
- `REMARD.md`

不建议上传：

- `node_modules/`
- `.workbuddy/`
- `*.bak`
- 临时文件、日志文件、调试输出文件

## 本地校验

修改脚本后可执行：

```bash
node --check js/api.js
node --check js/popup.js
node --check js/background.js
```

全部通过后再提交到 GitHub。
